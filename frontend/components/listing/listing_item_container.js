import { connect } from 'react-redux'
import ListingItem from './listing_index'

const mapStateToProps = state => ({
 
})

const mapDispatchToProps = dispatch => ({
    
})

export default connect(mapStateToProps, mapDispatchToProps)(ListingItem)